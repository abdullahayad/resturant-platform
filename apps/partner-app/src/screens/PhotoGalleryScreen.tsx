import { PlaceholderScreen } from '../components/PlaceholderScreen';

export function PhotoGalleryScreen() {
  return (
    <PlaceholderScreen
      title="Photo Gallery"
      description="Food Album (All / Most Ordered / per-category), Menu Album, and Ambience Album (Outdoor / Indoor / Other)."
    />
  );
}
