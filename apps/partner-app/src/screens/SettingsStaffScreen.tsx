import { PlaceholderScreen } from '../components/PlaceholderScreen';

export function SettingsStaffScreen() {
  return (
    <PlaceholderScreen
      title="Settings & Staff"
      description="Account settings (password, notifications) and staff invite/role management."
    />
  );
}
