import { PlaceholderScreen } from '../components/PlaceholderScreen';

export function MenuManagementScreen() {
  return (
    <PlaceholderScreen
      title="Menu Management"
      description="Add, edit, and delete dishes: name (EN/AR), category, price, and photo."
    />
  );
}
